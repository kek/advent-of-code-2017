defmodule AdventOfCode2017.RingTest do
  use ExUnit.Case
  alias AdventOfCode2017.Ring

  describe "reduce" do
    # test "with no items" do
    #   ring = Ring.new([])

    #   assert Ring.reduce(ring, 0, fn _, _ -> -1 end) == 0
    # end

    # test "with three items" do
    #   ring = Ring.new([1, 1, 1])

    #   result = Ring.reduce(ring, 0, fn acc, item, next -> acc + item + next end)

    #   assert result == 6
    # end
  end
end
